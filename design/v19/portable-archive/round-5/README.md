# The portable archive — round 5 (#1002): the heading set apart

Round 4 with one change, for the verdict (owner, 2026-09-20): "This makes
it look like it's three tabs but the archive is just a heading." The
heading and the tabs shared one voice — mono uppercase, faint — so the eye
read three tabs. Now they do not:

- THE ARCHIVE keeps the card heading's grammar (`.card h3`), unchanged.
- The tabs take the panels' own h4 voice — `600 12.5px var(--display)`,
  sentence case — chosen tab in `--ink` with the 2px `--accent` rule,
  the other `--ink-faint`; and they stand behind a 1px `--line` hairline,
  22px in from the heading.
- Under 560px the tabs wrap to their own line and the hairline goes.

Nothing else moved: panels, copy, manifest, fields, keyboard and aria are
round 4 verbatim. This is the tab grammar the next v19 surface copies.

Served at
`http://<LAN address>:8336/1002-portable-archive/design/v19/portable-archive/round-5/e-heading-apart.html`,
same scenes as round 4.

## Verdict

Owner, 2026-09-20: "68 still unclear." — the heading still read as a tab.
Answered in `../round-6/`.
