// GENERATED FILE -- do not edit by hand.
//
// PLACEHOLDER (#998): the third hold-out's twelve documents are written and
// verified in the commit right after this one. This empty array exists only
// so every eval CLI can be wired to `--holdout3` in the same commit that
// deletes the first two hold-out modules, without leaving the build broken
// in between. `scripts/corpus/generate.mjs --dir holdout3` overwrites this
// file with the real twelve once they exist; nothing here is ground truth.
//
// Score with `npm run eval:holdout -- --holdout3`. Regenerate with:
//
//   node scripts/corpus/generate.mjs --dir holdout3
//

import type { CorpusDocument } from "./extraction-corpus";

export const EXTRACTION_HOLDOUT3_FULLPAGE: CorpusDocument[] = [];
