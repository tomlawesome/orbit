DATABASE_URL=
DATABASE_URL_FILE=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
APP_URL=http://127.0.0.1:3000
ORBIT_BIND_ADDRESS=0.0.0.0
ORBIT_PORT=3000
SESSION_SECRET=
SESSION_SECRET_FILE=.orbit-secrets/session-secret
SESSION_TTL_SECONDS=604800
DOCUMENTS_ROOT=/var/lib/orbit/documents
DOCUMENTS_QUARANTINE_ROOT=/tmp/orbit-document-quarantine
DOCUMENT_KEK=
DOCUMENT_KEK_FILE=.orbit-secrets/document-kek
DOCUMENT_MAX_BYTES=26214400
DOCUMENT_HOUSEHOLD_QUOTA_BYTES=5368709120
DOCUMENT_INSTANCE_QUOTA_BYTES=21474836480
DOCUMENT_RETENTION_DAYS=30
DOCUMENT_SCAN_MODE=required
CLAMAV_HOST=orbit-clamav
CLAMAV_PORT=3310
CLAMAV_TIMEOUT_MS=30000
TIKA_URL=
TIKA_TIMEOUT_MS=45000
TIKA_MEMORY_LIMIT=1g
# Used only by docker-compose.full.yml's private Ollama service. Orbit does not
# currently call Ollama, so selecting/pulling a model is an explicit operator
# action rather than a normal deployment requirement.
OLLAMA_MODEL=
OLLAMA_MEMORY_LIMIT=6g
OLLAMA_CPUS=2.0
OLLAMA_MAX_QUEUE=8
OLLAMA_KEEP_ALIVE=0
CLAMAV_MEMORY_LIMIT=4g
OIDC_ISSUER=https://auth.example.com/application/o/orbit/
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_CLIENT_SECRET_FILE=
OIDC_CALLBACK_URL=http://127.0.0.1:3000/api/auth/callback
OIDC_SCOPES=openid profile email
OIDC_EMAIL_CLAIM=email
OIDC_EMAIL_VERIFIED_CLAIM=email_verified
OIDC_NAME_CLAIM=name
OIDC_AVATAR_CLAIM=picture
# Preferred SMTP configuration. STARTTLS is required by default; use
# implicit_tls only for providers that require TLS from connection start (465).
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USER=
SMTP_PASSWORD=
SMTP_PASSWORD_FILE=
# Deprecated compatibility form; leave empty when using the fields above.
SMTP_URL=
SMTP_URL_FILE=
SMTP_FROM=Orbit <orbit@example.com>
IMAP_HOST=
IMAP_PORT=993
IMAP_USER=
IMAP_PASSWORD=
IMAP_PASSWORD_FILE=
IMAP_MAILBOX=INBOX
IMAP_TLS_SERVER_NAME=
IMAP_RECIPIENT_DOMAIN=
IMAP_ALIAS_SECRET=
IMAP_ALIAS_SECRET_FILE=
IMAP_TRUSTED_RECIPIENT_HEADER=X-Original-To
IMAP_POLL_SECONDS=300
VAPID_SUBJECT=mailto:admin@example.com
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_PRIVATE_KEY_FILE=
WORKER_POLL_SECONDS=60
NOTIFICATION_MAX_ATTEMPTS=5
MIGRATE_ON_START=false
WORKER_ENABLED=false
DRIZZLE_MIGRATIONS_PATH=drizzle
ORBIT_SECRETS_DIR=./.orbit-secrets
POSTGRES_DB=orbit
POSTGRES_USER=orbit
POSTGRES_PASSWORD=
POSTGRES_PASSWORD_FILE=.orbit-secrets/postgres-password
